import { createTheme } from "@mui/material/styles";

export const themeDark = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#e91e63",
    },
    secondary: {
      main: "#f50057",
    },
  },
});
